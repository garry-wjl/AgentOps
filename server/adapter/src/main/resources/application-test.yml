server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agentops_test?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379
      database: 1

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

agentops:
  auth:
    token-ttl-seconds: 7200