server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agent_ops?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 123456

  data:
    redis:
      host: localhost
      port: 6379
      database: 0

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

agentops:
  auth:
    token-ttl-seconds: 7200